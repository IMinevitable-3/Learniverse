import { ImageSourcePropType } from "react-native";

const images: { [key: string]: ImageSourcePropType } = {
  bookImageMine: require("../assets/images/address-book.png"),
  icon: require("../assets/images/icon.png"),
};

export default images;
